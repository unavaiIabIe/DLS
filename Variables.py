import pygame
size = width, height = 1920, 1080
speed = [1, 1]
screen = pygame.display.set_mode(size, pygame.FULLSCREEN)
pygame.font.init()
font = pygame.font.Font(pygame.font.get_default_font(), 10)
big_font = pygame.font.Font(pygame.font.get_default_font(), 20)
